spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TodoProject?createDatabaseIfNotExist=true
    username: root
    password: slifer@Klk2020
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  flyway:
    enabled: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

jwt:
  secret: bXktdmVyeS1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW5z
  expiration: 86400000

